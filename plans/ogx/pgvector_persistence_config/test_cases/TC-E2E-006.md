---
test_case_id: TC-E2E-006
source_key: RHAIENG-6374
objectives: [4]
priority: P1
status: Draft
automation_status: Not Started
last_updated: "2026-09-21"
upgrade_phase: post
---
# TC-E2E-006: Release gate: pkg/config unit test asserts the pgvector persistence block

**Objective**: The operator build under test carries a `pkg/config` unit test asserting the
pgvector `persistence` block, and it passes.

**Preconditions**:

- All preconditions of TC-E2E-005

**Test Steps**:

1. Confirm `func TestExpandProviders_PgvectorPersistence` exists in `pkg/config/config_test.go`
2. Confirm that test asserts both `persistence["backend"]` and `persistence["namespace"]`
3. Run `go test ./pkg/config/... -run TestExpandProviders_PgvectorPersistence -v`
4. Run `go test ./pkg/config/... -run 'TestExpandProviders_Pgvector' -v`

**Expected Results**:

- Step 1 prints one matching line
- Step 2 prints at least one line checking `"backend"` and one checking `"namespace"`
- Step 3 output contains `--- PASS: TestExpandProviders_PgvectorPersistence` and ends with `ok`
- Step 4 ends with `ok` and contains no `--- FAIL` lines
